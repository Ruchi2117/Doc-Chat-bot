# === API Configuration ===
API_HOST=127.0.0.1
API_PORT=8000

# === Model Configuration ===
LLAMA_MODEL_PATH=backend/models/llama-2-7b-chat.Q4_K_M.gguf
MAX_NEW_TOKENS=512
TEMPERATURE=0.7
TOP_P=0.95

# === Vector Store ===
DOCUMENTS_DIR=backend/documents
VECTORSTORE_PATH=backend/vectorstore
CHUNK_SIZE=500

# === Embedding Model ===
EMBEDDING_MODEL=all-MiniLM-L6-v2

# === Logging ===
LOG_LEVEL=INFO

# === API Keys ===
GROQ_API_KEY=your_api_key_here

# === Instructions ===
# 1. Copy this file to '.env' in the same directory
# 2. Replace 'your_api_key_here' with your actual Groq API key
# 3. Adjust other settings as needed
# 4. Make sure .env is in your .gitignore to keep your API key secure